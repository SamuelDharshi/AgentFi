# Agents Folder

This folder is reserved for optional standalone agent runners, simulation scripts, or prompt packs.

Current prototype wires agents inside backend service modules:

- `backend/src/agents/userAgent.ts`
- `backend/src/agents/marketAgent.ts`
- `backend/src/agents/communication.ts`
